import React from 'react'

const Title = ({title, subtitle}) => {
  return (
    <TitleWrapperapper>
      <h1>{title}</h1>
      <p>{subtitle}</p>
    </TitleWrapperapper>
  )
}

export default Title
